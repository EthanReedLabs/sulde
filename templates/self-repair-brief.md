# Sulde L3 任务书

## 目标
<一句话说明要达成的可验证结果>

## 范围
涉及路径：<明确列出>

禁止改动：破坏性 git 操作；提交或推送；删除任务范围外文件；修改全局配置；<任务特定禁止项>

## 已知上下文
<只写已验证事实、一手证据路径、已证伪方案及失败原因>

## 意图契约
执行器会从本任务书生成 revisioned intent contract，并持续监督
`intent → Skill → MCP/tool → side effect → evidence`。Skill 不扩大权限；Codex 使用
Skill 时须显式登记开始/结束。任何 MCP 写入或外部写入都必须逐事件获人批准并在写后独立回读；
未闭合 Skill、开放调用或验证债务均不得判成功。
语义审查会产生额外 provider 调用，默认关闭；只有任务发射者显式选择
`--guardian-critic` 并接受改动片段在密钥扫描后送往当前 provider 时才启用。

## 执行效率要求
先做最小验证性探索，再实施最小充分改动；机械检查尽量合并执行。

## 日志文件
执行协议会把完整终态报告保存到隔离 worktree 的 `.codex-agent/<slug>.last.md`；
`.codex-agent` 是为兼容既有验收记录保留的 Sulde 协议目录，不代表执行提供方。

## 完成标准
<逐条列出可执行验证。每个 ✅ 必须在同一行包含结论、命令原文、exit code 与输出摘要；没有证据不要标 ✅>

## 体量与熔断
本轮控制在约 10 分钟内；同一失败修复三次仍未通过时停止，并如实报告当前状态。
若监督器判定确定性越界或高置信语义漂移，立即停止 provider，保留 worktree 和
`paused` 契约；不得绕过门禁、自己恢复或换另一条 Skill/MCP 路线重复执行。

**汇报硬性要求：必须五段标题（## 结果／## 过程／## 遇到的问题／## 解决方式／
## 遗留风险与建议），完成标准逐条 ✅/❌ 且每个 ✅ 同行附命令原文（反引号包裹）+
exit code + 输出摘要；缺任一段即视为未完成。**

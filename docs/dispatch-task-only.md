# Codex 普通派单默认只输出任务正文

## 结果与范围

基线 `dev@f687193`，任务分支 `task/dispatch-task-only`。
用户要求普通派单不再附带模型、推理切换或“无需切换”提示。

- 生成器的默认 Codex 文本和 JSON instructions 仅含任务指令；不发出模型动作，
  当前配置未知也不阻止派单。`ready` 仅表示可生成派单，不证明模型能力。
- `--model-advice` 是显式建议入口；任务能力档本身不是用户请求模型建议的证据。
- 派单、协调、接单 Skill 与新项目模板同步默认行为。
- 安装 smoke 验证任务正文输出和零模型动作，防止未来发布复发。
- 后台受管 Agent 的模型选择与另一宿主行为保持不变。

## 本机配置与发布边界

当前全局 AGENTS 的派单段已更新，并与源码模板的该段逐字比较一致。
全局文件的其他段落未变化；旧安装生成器或 Skill 的前缀要求由这一明确偏好覆盖。

- 变更前全局文件 SHA256：`c01fce4603c35f897e8eda3ab6c1f429956c3935a717b9b74ca691517132585a`
- 变更后全局文件 SHA256：`d753e6c0ab0b68eb3ca64fe0a9f85bdf3561e8589c2c6920e3f1012901c8cb85`
- 原文只读备份：任务 worktree 的 `.sulde/data/dispatch-task-only/global-agents.before.md`，
  模式 0400，仅本地保存，不加入 Git 或发布件。Git 不保留该只读权限，不能以提交代替备份。
- 没有修改已安装缓存、runtime、生产安装、main、dev 或 LIFE 返修工作树。

**生成器与 Skill 的发布尚未进行。** 后续发布前须将本任务源码纳入实际发布树，
否则旧 dev 的安装器仍可能重写全局派单段。已运行的其他会话也未证明会自动重载全局规则。

## 测试证据

- 红例：新增默认模式/显式建议 CLI 两项测试在旧实现上失败，确认旧默认产生模型切换。
- 修改后 `tests.test_runtime_provider` 与 `tests.test_model_dispatch_contract` 共 28 项通过。
- dispatch-task 与 writing-task-md 的官方 Skill 校验通过。
- assign 的官方通用校验因既有 `user-invocable` 字段拒绝；该字段在 dev 已存在，
  本次仅改正文，未为单一宿主校验删除跨宿主元数据。
- 安装器、派单契约和运行时提供方共 82 项隔离影响测试通过（exit 0，255.635 秒）。
  回执和日志：`.sulde/data/test-evidence/20260907T144712.856741-b74b13184a99.{json,log}`；
  日志 SHA256：`9a6fa4122da673fca746515b0ecd3cc27468618f5e170d0efe657e3e13095414`。
  这包含隔离安装 smoke 的任务正文检查，不是生产安装；未执行仓库全量测试。
- 首次证据运行在测试启动前被嵌套系统沙盒拒绝（exit 3），不是代码测试失败或成功；
  保留 `20260907T144521.864175-3e3147820553` 的失败记录，随后以原生批准启动系统隔离测试。

## 沉淀候选

证据状态：verified（默认输出回归及全局配置回读），已安装新版生成器验收未执行。
问题：对话中的格式偏好没有同步全局规则、派单生成器、接单消费者及安装模板，下一次派单会复发。

- 路由正例：用户明确取消普通派单模型建议，而旧模板仍要求固定前缀。
- 路由反例：用户明确要求模型选择建议，此时保留可选能力，不删除后台模型策略。
- 执行正例：默认零模型动作，显式 opt-in，当前全局规则与源码一致；测试旧实现确实失败。
- 执行反例：只口头答应、直接改摘要保护缓存、改动后台 Agent 模型或称未部署源码已安装。

本候选仅供单写者判重，未直接修改事实知识库。

# Extension SDK

This directory is intentionally empty except for its registry and examples generated on demand.
Use `scripts/sulde add-skill`, `add-hook`, `add-check`, or `add-knowledge-container` in a fork.
Generators refuse to overwrite existing files and registry paths are constrained to the plugin root.

Extensions are local code. Review them like any other executable code before enabling them.

# Hook extensions

Generated hook modules live here and are called by the three existing Community hook entrypoints.
Each module exposes `run(config, payload)` and is registered once in `extensions/registry.json`.
